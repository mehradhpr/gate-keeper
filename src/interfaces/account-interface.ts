export interface ServerAccountInfo {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  password: string;
  role: string;
}

export interface ClientAccountInfo {
  firstName: string;
  lastName: string;
  email: string;
  role: string;
}
